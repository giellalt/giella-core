## Process this file with automake to produce Makefile.in
## Copyright: Sámediggi/Divvun/UiT
## Licence: GPL v3+

######## Test targets: ###########

# Do not run tests in this directory in parallel, it can crash the system.
# The tests are run by Python, and the tests are parallelised there, so
# there is no need to further parallelise in make, to the contrary.
.NOTPARALLEL:

if WANT_GRAMCHECK

# Default variable for all languages
LOG_COMPILER=gtgramtool
AM_LOG_FLAGS=test --hide-passes --spec ../$(GTLANG2).zcheck yaml

endif # WANT_GRAMCHECK
