! Remove word boundary mark from the lower side of the transducer:
! 😱 or ∑ is used in some languages to prefix # in dynamic compounds.

 %#  -> 0,
"##" -> 0,
"😱" -> 0,
"∑"  -> 0,

! Also convert escaped symbols to their real surface form:
"#7" -> %#;
